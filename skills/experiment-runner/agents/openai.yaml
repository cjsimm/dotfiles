interface:
  display_name: "Experiment Runner"
  short_description: "Execute one approved, traceable experiment run"
  default_prompt: "Use $experiment-runner to conduct the supplied isolated experiment run through its human-owned gates."
policy:
  allow_implicit_invocation: false
