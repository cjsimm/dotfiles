return {}
-- return {
--     dir = "/Users/gb/projects/seashells",
--     name = "seashells",
--     lazy = false,
--     priority = 1000,
--     config = function()
--         vim.cmd.colorscheme("SeaShells")
--     end,
-- }

-- return {
--     -- pull in the local repo instead of the github repo for now
--     lazy = false,
--     priority = 1000
-- }
